hallo

## Setup directories

Run the `create_folders.ps1` script to setup the directory structure.

## Input files

To run the solutions on your own data, download your data as `input.txt` and put it in the designated folder. As these are personal they are ignored by git.